source "https://rubygems.org"

# github-pages pulls in the same Jekyll version and plugins that GitHub Pages uses,
# so the local build matches what gets deployed.
gem "github-pages", group: :jekyll_plugins

# webrick is required for Ruby 3.x (removed from stdlib in 3.0)
gem "webrick"
